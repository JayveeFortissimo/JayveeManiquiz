import type { Metadata } from "next";
import { ThemeProvider } from "@/components/theme-provider";
import "./globals.css";


export const metadata: Metadata = {
  title: "Jayvee Maniquiz - Web Developer",
  description: "Generated by create next app",
    icons: {
    icon: "/image/Profile.jpg", // path relative to public/
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
    
      <body
      className="inter"
      >
      <ThemeProvider
            attribute="class"
            defaultTheme="system"
            enableSystem
            disableTransitionOnChange
          >
            {children}
            <footer className="container mx-auto max-w-[50rem] min-h-[5rem] flex justify-between items-center border  border-t-gray-300 border-l-0 border-r-0 px-2 md:px-0 mt-3">
              <p className="text-[0.9rem]">maniquizjayvee.Business@gmail.com</p>
              <p className="text-[0.9rem]">Feed Back</p>
              </footer>
          </ThemeProvider>
      </body>
    </html>
  );
}

